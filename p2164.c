#include <stdio.h>
#include <stdlib.h>

typedef struct s_node
{
	int n;
	struct s_node *next;
}	t_node;

int main()
{
	int *s_node;

	scanf("%d", &n);
	for (int i=0;i<n;i++)
	{
		l[i] = i+1;
	}
	for (int i=0;i<n;i++)
	{
		printf("%d\n", l[i]);
	}
}
